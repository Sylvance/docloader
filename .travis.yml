language: ruby

rvm:
  - ruby-2.5.5

before_script:
  - bundle exec rake db:create
  - bundle exec rake db:migrate

script:
  - bundle exec rake test

services:
  - postgresql

addons:
  postgresql: "9.4"
