language: ruby

rvm:
  - ruby-2.5.5

before_script:
  - gem install bundler
  - bundle install --jobs=3 --retry=3
  - bundle exec rake db:create
  - bundle exec rake db:migrate

script:
  - rake test

cache: bundler

services:
  - postgresql

addons:
  postgresql: "9.4"
