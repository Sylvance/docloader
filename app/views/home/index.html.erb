<div class="jumbotron">
  <h1 class="display-4">Docloader</h1>
  <p class="lead">Your files are safe with us. Welcome to give it a try.</p>
  <hr class="my-4">
  <p>Docloader allows you to upload your files to the cloud and download them later.</p>
  <% if current_user %>
    <%= link_to "View documents", posts_path, class: "btn btn-info btn-lg", role: "button" %>
  <% else %>
    <%= link_to "Login", login_path, class: "btn btn-primary btn-lg", role: "button" %>
    <%= link_to "Sign Up", signup_path, class: "btn btn-success btn-lg", role: "button" %>
  <% end %>
</div>
